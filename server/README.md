# InterviewToast Server

Firstly, install the project dependencies using npm

```bash
npm i
```

Secondly, make sure you create a `.env` following the format provided in the `.env-sample`.

Thirdly, start the server and happy hacking!

```bash
npm start
```
